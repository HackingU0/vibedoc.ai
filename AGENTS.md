# Repository instructions

Read and follow `CLAUDE.md`; it is the canonical project context and architecture guide.

Use Python 3.13 with `uv`. Run the local checks with:

```sh
uv run python -m tests.test_core
```
